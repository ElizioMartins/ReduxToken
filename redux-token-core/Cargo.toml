[package]
name = "redux-token-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "redux_token_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
serde_json = "1"
regex = "1"
